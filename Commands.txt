AWS EC2


------- STEPS TO CONNECT AN INSTANCE -------
1) Install the CLI
https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html

2) Download the pem and assing the permission to file
icacls "your-key.pem" /inheritance:r
icacls "your-key.pem" /grant:r "%username%":"(R)"

3) Connect to the instance
ssh -i "yout-key.pem" secret_server_intance


------- STEPS TO INSTALL DOCKER ON UBUNTU ------- (https://docs.docker.com/engine/install/ubuntu/)
1) Uninstall conflict files
sudo apt remove $(dpkg --get-selections docker.io docker-compose docker-compose-v2 docker-doc podman-docker containerd runc | cut -f1)

2) Setup apt repository
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

3) Add the repository to Apt sources
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}")
Components: stable
Signed-By: /etc/apt/keyrings/docker.asc
EOF

- Update newly the apt repository
sudo apt update

4) Install docker engine (automaticlly starts)
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

5) (Optional) Verify running
sudo systemctl status docker

- Iniciar en caso de que no se haya ejecutado
sudo systemctl start docker

- Install the docker compose component
sudo apt install docker-compose

- Initialice the component
sudo docker-compose -f docker-compose.sqlserver.yml up -d

------- STEPS TO DEPLOY AN .NET APPLICATION (https://learn.arm.com/learning-paths/servers-and-cloud-computing/net-aspire/aws/) -------
1) Connect to an AWS EC2 Instance (ssh)

2) Update the components apt
sudo apt update && sudo apt upgrade -y

3) Install the .NET environment (https://ubuntuhandbook.org/index.php/2025/07/install-net-10-ubuntu-ppa/)
wget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
sudo apt update
sudo apt install -y dotnet-sdk-9.0

4) Verify the instance created
dotnet --version

5) Restore the packages (into de project folder)
sudo dotnet restore

6) Configure and install the Aspire Environment (into de project folder)
dotnet tool install --global Aspire.Cli --version 9.5.2




https://ubuntuhandbook.org/index.php/2025/07/install-net-10-ubuntu-ppa/
dotnet --list-sdks

Ubuntu Version
lsb_release -a
sudo apt remove package_name

